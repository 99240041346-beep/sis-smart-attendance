const { Pool } = require('pg');
const pool = new Pool({connectionString:process.env.DATABASE_URL,ssl:process.env.NODE_ENV==='production'?{rejectUnauthorized:false}:false,max:Number(process.env.DB_POOL_MAX||10)});
pool.on('error',err=>console.error('PostgreSQL pool error',err));
const query=(text,params)=>pool.query(text,params);
async function transaction(work){const client=await pool.connect();try{await client.query('BEGIN');const result=await work(client);await client.query('COMMIT');return result}catch(e){await client.query('ROLLBACK');throw e}finally{client.release()}}
async function ensureSchema(){
 await query('CREATE EXTENSION IF NOT EXISTS pgcrypto');
 await query(`CREATE TABLE IF NOT EXISTS app_users(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),email TEXT UNIQUE NOT NULL,password_hash TEXT NOT NULL,name TEXT NOT NULL,role TEXT NOT NULL CHECK(role IN ('student','faculty','admin')),register_number TEXT UNIQUE,employee_id TEXT UNIQUE,department TEXT,programme TEXT,academic_year TEXT,semester TEXT,phone TEXT,address TEXT,gender TEXT,dob DATE,blood_group TEXT,parent_name TEXT,parent_phone TEXT,latitude DOUBLE PRECISION,longitude DOUBLE PRECISION,face_template JSONB,profile_photo_url TEXT,face_enrollment_id TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT now())`);
 const userCols=[['profile_photo_url','TEXT'],['face_enrollment_id','TEXT'],['programme','TEXT'],['academic_year','TEXT'],['semester','TEXT'],['phone','TEXT'],['address','TEXT'],['gender','TEXT'],['dob','DATE'],['blood_group','TEXT'],['parent_name','TEXT'],['parent_phone','TEXT']];
 for(const[c,t]of userCols)await query(`ALTER TABLE app_users ADD COLUMN IF NOT EXISTS ${c} ${t}`);
 await query(`CREATE TABLE IF NOT EXISTS academic_terms(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),academic_year TEXT NOT NULL,semester TEXT NOT NULL,start_date DATE,end_date DATE,is_current BOOLEAN NOT NULL DEFAULT false,UNIQUE(academic_year,semester))`);
 await query(`CREATE TABLE IF NOT EXISTS subjects(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),code TEXT UNIQUE NOT NULL,name TEXT NOT NULL,department TEXT,credits NUMERIC(4,1) DEFAULT 0,category TEXT,faculty_id UUID REFERENCES app_users(id),created_at TIMESTAMPTZ NOT NULL DEFAULT now())`);
 await query(`CREATE TABLE IF NOT EXISTS enrollments(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),student_id UUID NOT NULL REFERENCES app_users(id) ON DELETE CASCADE,subject_id UUID NOT NULL REFERENCES subjects(id) ON DELETE CASCADE,academic_term_id UUID REFERENCES academic_terms(id),status TEXT NOT NULL DEFAULT 'Registered',UNIQUE(student_id,subject_id,academic_term_id))`);
 await query(`CREATE TABLE IF NOT EXISTS course_registrations(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),student_id UUID NOT NULL REFERENCES app_users(id) ON DELETE CASCADE,subject_id UUID REFERENCES subjects(id),term_id UUID REFERENCES academic_terms(id),status TEXT NOT NULL DEFAULT 'Registered',registered_at TIMESTAMPTZ NOT NULL DEFAULT now())`);
 await query(`CREATE TABLE IF NOT EXISTS timetable(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),subject_id UUID REFERENCES subjects(id),faculty_id UUID REFERENCES app_users(id),class_name TEXT NOT NULL,day_of_week INTEGER NOT NULL CHECK(day_of_week BETWEEN 0 AND 6),start_time TIME NOT NULL,end_time TIME NOT NULL,room TEXT)`);
 await query(`CREATE TABLE IF NOT EXISTS marks(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),student_id UUID NOT NULL REFERENCES app_users(id) ON DELETE CASCADE,subject_id UUID REFERENCES subjects(id),term_id UUID REFERENCES academic_terms(id),assessment TEXT NOT NULL,marks NUMERIC(6,2),max_marks NUMERIC(6,2),updated_at TIMESTAMPTZ NOT NULL DEFAULT now())`);
 await query(`CREATE TABLE IF NOT EXISTS grades(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),student_id UUID NOT NULL REFERENCES app_users(id) ON DELETE CASCADE,subject_id UUID REFERENCES subjects(id),term_id UUID REFERENCES academic_terms(id),grade TEXT,grade_point NUMERIC(4,2),credits NUMERIC(4,1),result_status TEXT)`);
 await query(`CREATE TABLE IF NOT EXISTS non_cgpa_activities(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),student_id UUID NOT NULL REFERENCES app_users(id) ON DELETE CASCADE,title TEXT NOT NULL,category TEXT,credits NUMERIC(4,1),group_no TEXT,mark TEXT,year_of_passing TEXT,status TEXT DEFAULT 'Pending')`);
 await query(`CREATE TABLE IF NOT EXISTS fees(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),student_id UUID NOT NULL REFERENCES app_users(id) ON DELETE CASCADE,fee_type TEXT NOT NULL,term_id UUID REFERENCES academic_terms(id),amount NUMERIC(12,2) NOT NULL DEFAULT 0,due_date DATE,status TEXT NOT NULL DEFAULT 'Pending',receipt_no TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT now())`);
 await query(`CREATE TABLE IF NOT EXISTS payments(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),student_id UUID NOT NULL REFERENCES app_users(id),fee_id UUID REFERENCES fees(id),amount NUMERIC(12,2) NOT NULL,provider TEXT,transaction_id TEXT UNIQUE,status TEXT NOT NULL DEFAULT 'Pending',paid_at TIMESTAMPTZ)`);
 await query(`CREATE TABLE IF NOT EXISTS results(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),student_id UUID NOT NULL REFERENCES app_users(id),term_id UUID REFERENCES academic_terms(id),sgpa NUMERIC(5,2),cgpa NUMERIC(5,2),earned_credits NUMERIC(6,2),published_at TIMESTAMPTZ)`);
 await query(`CREATE TABLE IF NOT EXISTS circulars(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),title TEXT NOT NULL,body TEXT,category TEXT,published_at TIMESTAMPTZ NOT NULL DEFAULT now(),published BOOLEAN NOT NULL DEFAULT false)`);
 await query(`CREATE TABLE IF NOT EXISTS course_feedback(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),student_id UUID NOT NULL REFERENCES app_users(id),subject_id UUID REFERENCES subjects(id),rating INTEGER CHECK(rating BETWEEN 1 AND 5),comments TEXT,submitted_at TIMESTAMPTZ NOT NULL DEFAULT now(),UNIQUE(student_id,subject_id))`);
 await query(`CREATE TABLE IF NOT EXISTS bookings(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),student_id UUID NOT NULL REFERENCES app_users(id),booking_type TEXT NOT NULL CHECK(booking_type IN ('hostel','transport')),details JSONB NOT NULL DEFAULT '{}'::jsonb,status TEXT NOT NULL DEFAULT 'Pending',created_at TIMESTAMPTZ NOT NULL DEFAULT now())`);
 await query(`CREATE TABLE IF NOT EXISTS academic_events(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),title TEXT NOT NULL,event_date DATE NOT NULL,description TEXT,category TEXT)`);
 await query(`CREATE TABLE IF NOT EXISTS attendance_sessions(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),subject_id UUID REFERENCES subjects(id),faculty_id UUID NOT NULL REFERENCES app_users(id),class_name TEXT,qr_token_hash TEXT NOT NULL,faculty_latitude DOUBLE PRECISION NOT NULL,faculty_longitude DOUBLE PRECISION NOT NULL,allowed_radius_m INTEGER NOT NULL DEFAULT 50 CHECK(allowed_radius_m BETWEEN 5 AND 1000),expires_at TIMESTAMPTZ NOT NULL,active BOOLEAN NOT NULL DEFAULT true,created_at TIMESTAMPTZ NOT NULL DEFAULT now())`);
 await query(`CREATE TABLE IF NOT EXISTS attendance_records(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),session_id UUID NOT NULL REFERENCES attendance_sessions(id) ON DELETE CASCADE,student_id UUID NOT NULL REFERENCES app_users(id),register_number TEXT NOT NULL,qr_verified BOOLEAN NOT NULL DEFAULT false,face_verified BOOLEAN NOT NULL DEFAULT false,liveness_verified BOOLEAN NOT NULL DEFAULT false,student_latitude DOUBLE PRECISION,student_longitude DOUBLE PRECISION,distance_m DOUBLE PRECISION,status TEXT NOT NULL DEFAULT 'Present' CHECK(status IN ('Present','Rejected')),rejection_reason TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT now(),UNIQUE(session_id,student_id))`);
 await query(`CREATE TABLE IF NOT EXISTS permission_requests(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),student_id UUID NOT NULL REFERENCES app_users(id) ON DELETE CASCADE,permission_type TEXT NOT NULL DEFAULT 'Class Permission',permission_date DATE NOT NULL,start_time TIME,end_time TIME,subject_id UUID REFERENCES subjects(id),reason TEXT NOT NULL,document_url TEXT,status TEXT NOT NULL DEFAULT 'Pending' CHECK(status IN ('Pending','Approved','Rejected','Cancelled')),faculty_id UUID REFERENCES app_users(id),faculty_remarks TEXT,admin_id UUID REFERENCES app_users(id),admin_remarks TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT now(),updated_at TIMESTAMPTZ NOT NULL DEFAULT now())`);
 await query(`CREATE INDEX IF NOT EXISTS idx_permission_student ON permission_requests(student_id,created_at DESC)`);
 await query(`CREATE INDEX IF NOT EXISTS idx_permission_status ON permission_requests(status,permission_date)`);
 await query(`CREATE TABLE IF NOT EXISTS audit_logs(id UUID PRIMARY KEY DEFAULT gen_random_uuid(),user_id UUID REFERENCES app_users(id),action TEXT NOT NULL,details JSONB,created_at TIMESTAMPTZ NOT NULL DEFAULT now())`);
 await query('CREATE INDEX IF NOT EXISTS idx_attendance_student ON attendance_records(student_id)');await query('CREATE INDEX IF NOT EXISTS idx_attendance_session ON attendance_records(session_id)');await query('CREATE INDEX IF NOT EXISTS idx_sessions_expiry ON attendance_sessions(expires_at)');
}
module.exports={pool,query,transaction,ensureSchema};
