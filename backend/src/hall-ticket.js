const express=require('express');const router=express.Router();const {query}=require('./db');const auth=require('./middleware/auth');router.use(auth);
router.get('/data',async(req,res,next)=>{try{const r=await query(`SELECT id,full_name,email,role,register_number,employee_id,department,profile_photo_url FROM app_users WHERE id=$1`,[req.user.id]);if(!r.rowCount)return res.status(404).json({error:'Profile not found'});res.json({student:r.rows[0],generatedAt:new Date().toISOString()})}catch(e){next(e)}});module.exports=router;
